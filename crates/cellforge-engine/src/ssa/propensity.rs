/// Propensity function calculations for stochastic reactions.
pub struct PropensityCalculator {
    _private: (),
}

impl PropensityCalculator {
    pub fn new() -> Self {
        Self { _private: () }
    }
}
