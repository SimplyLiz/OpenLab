/// Direct Gillespie SSA (Stochastic Simulation Algorithm) implementation.
pub struct GillespieSolver {
    _private: (),
}

impl GillespieSolver {
    pub fn new() -> Self {
        Self { _private: () }
    }
}
